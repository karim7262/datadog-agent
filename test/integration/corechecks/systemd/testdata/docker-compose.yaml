version: "3.5"

services:
  systemd2:
    privileged: true
    image: "centos/systemd:latest"
    volumes:
#      - ./container/run:/run
      - /tmp/var/run:/var/run:rw
#      - /tmp/run/:/run:rw
#      - ./system.conf:/etc/systemd/system.conf
